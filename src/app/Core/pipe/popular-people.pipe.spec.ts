import { PopularPeoplePipe } from './popular-people.pipe';

describe('PopularPeoplePipe', () => {
  it('create an instance', () => {
    const pipe = new PopularPeoplePipe();
    expect(pipe).toBeTruthy();
  });
});
